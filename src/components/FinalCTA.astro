---
import { RESERVATION_ANCHOR, site } from '../data/site';
---

<section class="bg-hueso py-20 sm:py-28">
  <div class="mx-auto max-w-6xl px-5 sm:px-8">
    <div
      class="relative overflow-hidden rounded-xl2 bg-albero px-7 py-14 text-center text-rio sm:px-12 sm:py-20"
    >
      <div class="bg-grain pointer-events-none absolute inset-0 opacity-40" aria-hidden="true"></div>
      <div
        class="pointer-events-none absolute inset-0 bg-[radial-gradient(80%_120%_at_50%_-20%,rgba(246,241,231,0.25),transparent_60%)]"
        aria-hidden="true"
      ></div>

      <div class="relative">
        <h2 class="mx-auto max-w-2xl text-balance font-display text-3xl font-600 leading-tight sm:text-4xl lg:text-5xl">
          ¿Tu perro necesita un sitio de confianza en Mula?
        </h2>
        <p class="mx-auto mt-5 max-w-xl text-pretty text-lg text-rio/80">
          Envía tu solicitud de reserva y te confirmamos personalmente. Sin
          compromiso.
        </p>
        <div class="mt-9 flex flex-col items-center justify-center gap-3 sm:flex-row">
          <a
            href={RESERVATION_ANCHOR}
            class="inline-flex items-center justify-center gap-2.5 rounded-full bg-hueso px-8 py-4 text-base font-700 text-rio shadow-lg transition-all hover:shadow-xl hover:shadow-rio/25"
          >
            Solicitar reserva
            <span aria-hidden="true">→</span>
          </a>
        </div>
        <p class="mt-7 text-sm text-rio/80">
          Estamos en {site.addressDisplay}.
        </p>
      </div>
    </div>
  </div>
</section>
