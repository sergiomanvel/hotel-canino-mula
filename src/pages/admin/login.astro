---
import '../../styles/global.css';

export const prerender = false;

const hasError = Astro.url.searchParams.get('error') === '1';
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="robots" content="noindex, nofollow" />
    <title>Acceso · Hotel Canino Río Mula</title>
  </head>
  <body class="min-h-screen bg-cream-deep text-bark">
    <main class="mx-auto flex min-h-screen max-w-md flex-col justify-center px-5 py-12">
      <div class="rounded-xl2 border border-bark/10 bg-cream p-8 shadow-sm">
        <p class="mb-2 text-sm font-700 uppercase tracking-[0.16em] text-clay">Panel privado</p>
        <h1 class="font-display text-3xl font-600 text-forest">Iniciar sesión</h1>
        <p class="mt-3 text-pretty text-bark/75">
          Acceso privado para la gestión de reservas.
        </p>

        {hasError && (
          <p
            role="alert"
            class="mt-5 rounded-xl border border-clay/40 bg-clay/10 px-4 py-3 text-sm font-600 text-bark"
          >
            No hemos podido iniciar sesión con esos datos.
          </p>
        )}

        <form method="POST" action="/api/admin/login" class="mt-6 grid gap-5">
          <div class="flex flex-col gap-2">
            <label for="email" class="font-600 text-forest">Email</label>
            <input
              id="email"
              name="email"
              type="email"
              autocomplete="email"
              required
              class="rounded-xl border border-bark/20 bg-white px-4 py-3 text-bark outline-none focus:border-clay"
            />
          </div>

          <div class="flex flex-col gap-2">
            <label for="password" class="font-600 text-forest">Contraseña</label>
            <input
              id="password"
              name="password"
              type="password"
              autocomplete="current-password"
              required
              class="rounded-xl border border-bark/20 bg-white px-4 py-3 text-bark outline-none focus:border-clay"
            />
          </div>

          <button
            type="submit"
            class="mt-1 inline-flex items-center justify-center gap-2 rounded-full bg-forest px-7 py-3.5 text-base font-700 text-cream shadow-sm transition-all hover:bg-forest-700"
          >
            Entrar
          </button>
        </form>
      </div>

      <p class="mt-6 text-center text-sm text-bark/60">
        <a href="/" class="font-600 text-clay underline-offset-4 hover:underline">← Volver a la web</a>
      </p>
    </main>
  </body>
</html>
